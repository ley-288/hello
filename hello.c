#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string name = get_string("What is your name?\n");                   //String = 'name'. Get string by asking prompted question.
    printf("Hello, %s\n", name);                                        //Say hello + the now gained String. ie User name. write comma Name to show computer this is the
}                                                                       //                                                                           string we need here.